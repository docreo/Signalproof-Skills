# `log-build-git` V0.1 Candidate Acceptance Evidence — Knowledge Forge RD0.3B

**Status:** REAL-WORKFLOW ACCEPTANCE EVIDENCE / CANDIDATE REMAINS NOT ACTIVE  
**Date:** 2026-08-18  
**Candidate:** `log-build-git` 0.1.0-candidate  
**Work stream:** Signalproof Knowledge Forge RD0.3B

## Purpose

Exercise the ordered candidate command:

`log-skill -> design-git -> build-git`

on a real open work stream where global Build Ledger chronology advanced independently, the next action was evidence inspection rather than code implementation, and prior failure intelligence changed the retry path.

## Phase 1 — log-skill

The Knowledge Forge RD0.3B stream remained OPEN.

Current meaningful state was synchronized without substituting `log-skill close`:

- RD0.3B staged evidence remains noncanonical pending chain-safe ingestion;
- no Build Ledger sequence was reserved;
- no canonical JSONL/SQLite mutation was performed by this workflow;
- no skill candidate was promoted to Active;
- the protected prior artifact and governance boundaries remained unchanged.

## Global chronology separation

While the Knowledge Forge stream was paused, unrelated canonical Build Ledger events advanced the global head.

The workflow correctly treated prior projected sequence labels as stale planning notation and repeatedly re-bound to the current live head rather than forcing RD0.3B into an old projected C-number.

This is direct evidence for the build-stream/global-ledger separation rule and `FI-LEDGER-STALE-SEQUENCE-001` prevention candidate.

## Known Failure Preflight

A PowerShell 5.1 parser defect occurred in a generated read-only inspection harness because of invalid multi-property `Sort-Object` syntax.

The failure was classified as a harness/parser failure, not a product or ledger failure. The block failed at parse time, so no canonical mutation occurred. The correction changed only the sorting syntax to a PowerShell 5.1-compatible property-expression form before re-execution.

The corrected gate passed and preserved the read-only boundary.

This provides recurrence-prevention evidence consistent with the PowerShell parser failure-intelligence family: parse/validate changed PowerShell harness syntax rather than blindly rerunning a known-bad form.

## design-git result

The evidence did not justify product implementation.

The bounded next action was designed as READ / INSPECT / VERIFY:

- inspect the staged RD0.3B wrapper records;
- inspect the native Build Ledger append API and schema contract;
- identify conditional-head protections and append semantics;
- preserve canonical ledger and protected product state;
- produce a non-mutating draft/closeout preview before any append decision.

## build-git result

`build-git` executed the routed inspection action only.

Observed results:

- RD0.3B staged material consists of two wrapper records: `StageSkills` and `MilestoneCloseout`;
- the staged records are not themselves canonical Build Ledger event objects;
- the native append implementation performs sanitization, normalization, validation, append locking, conditional head checks, sequence assignment, hash-chain construction, JSONL append, and SQLite index insertion;
- the schema requires canonical event fields including a project object and evidence/acceptance contract;
- the canonical ledger remained stable for the complete inspection;
- no product source, canonical ledger, or protected baseline was modified.

## Acceptance scenarios exercised

This real workflow materially supports:

- Scenario 2 — known recurring PowerShell failure changes route;
- Scenario 3 — query/examine only;
- Scenario 6 — open milestone remains open;
- Scenario 8 — paused build resumes after global ledger advancement;
- Scenario 10 — existing stale-sequence failure identity reused rather than creating a duplicate class.

It also partially supports current-state recheck behavior because the workflow repeatedly re-read volatile chronology instead of carrying forward stale projected state.

## Outcome

**PASS FOR THIS REAL-WORKFLOW CASE.**

The evidence supports the candidate semantics but does not satisfy the full promotion gate by itself. In particular, a materially different bounded implementation case is still required before `log-build-git` can be considered for Active promotion.

No Active promotion is authorized by this evidence record.
