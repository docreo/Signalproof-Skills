# Signalproof Workbench RD2 R6 G1 RD2 — Generated C# Backtick Serialization STOP

**Date:** 2026-08-21  
**Status:** C# COMPILE STOP / GENERATED-SOURCE SERIALIZATION / PRODUCTION UNCHANGED  
**Failure fingerprint:** `R6-G1-GENERATED-CS-BACKTICK-ESCAPE-001`  
**Attempt count:** 1

## Evidence-backed progress

The owner executed the R6-G1 RD2 launcher and the materially corrected builder passed all pre-compile gates:

- exact base builder SHA `BF605F672910E17BC4116FE4F13A8A68E319508DFB38AFBF073EE4072CA8E30A`;
- patched RD2 builder SHA `BC4A0A4D97C71B779222F2A3B560648B8C7100D244833CABE539B6AAB6301650`;
- parser preflight;
- fresh RD2 candidate lane;
- exact recovered R5.1 source;
- protected production identity;
- compiler and approved logo;
- Governor/no-bypass baseline;
- `AddMessage`, message payload, RichTextBox/transcript, Workbench Form, scroll, send, viewer identity, and `BuildShell` localization;
- generated G1 source SHA `4109D39F617C231BEAD5DC74A93DCBB0F57144C94CE8DF483C83E1BAA7014723`;
- artifact/version surfaces;
- no-bypass contract;
- Session/Bot preservation;
- R5.1 layout/scroll markers.

C# compilation then failed with `CS1056 Unexpected character '`'` at generated source lines 843 and 1581.

## Classification

`BUILDER / GENERATED-SOURCE SERIALIZATION`

The patch builder used single-quoted PowerShell strings containing `` `r`n ``. In single-quoted PowerShell strings these escape sequences are not expanded, so literal backtick characters were inserted into generated C# source.

The two known affected insertion surfaces are:

1. the `BuildShell` candidate-version badge patch;
2. the `SendCurrentPrompt` candidate-title/follow reset patch.

This is not a product behavior failure and not a C# logic failure. The compiler correctly rejected invalid generated bytes before runtime launch.

## Materially changed correction

R6-G1 RD3 must:

1. preserve RD1 and RD2 candidate/evidence lanes;
2. create a fresh RD3 candidate lane;
3. regenerate from the exact R5.1 recovered source through the exact G1 RD1 base builder;
4. remove the stale exact-subtitle requirement as in RD2;
5. replace both affected generated-source insertions with PowerShell construction using `[Environment]::NewLine` and ordinary quoted C# text, never literal PowerShell escape tokens;
6. assert the patched builder no longer contains the known single-quoted backtick insertion forms;
7. continue unchanged through no-bypass, Session/Bot, C# compile, self-test, production postflight, and launch-liveness gates.

## Failure-memory rule

Do not rerun RD2 unchanged. A new filename alone does not reset this fingerprint; the serialization method must change materially.

## Authority

The owner's active `/dsp complete` envelope remains valid for the bounded Workbench R6 stream.
