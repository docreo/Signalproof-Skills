# Signalproof Workbench R5 - Codex / Higher-Level Builder Complete Handoff

**Status:** HANDOFF READY / NEXT WORKSTREAM AUTHORIZED FOR BOUNDED `/dsp complete` START  
**Work stream:** Signalproof Workbench R5 Hermes-Approved Functionality Integration  
**Owner:** Doc Reo  
**Handoff branch:** `candidate/workbench-desktop-experience-r5`  
**Public Skills main last verified for this transfer:** `a1bf4633d1adc1a1229fc338daa45c57014d37c0`  
**Accepted UX baseline:** R5 Slice 1 RD2 top-navigation shell  
**Production mutation:** NONE AUTHORIZED BY THIS HANDOFF  
**Canonical private Build Ledger append:** NOT CLAIMED / SEPARATE AUTHORITY

---

# RECEIVING CODEX / HIGHER-LEVEL BUILDER - START HERE

Run the current Signalproof workflow as a fresh bounded completion stream:

```text
/dsp complete
```

But do not assume this packet is complete current truth.

Before consequential mutation:

1. refetch current `docreo/Signalproof-Skills/main`;
2. refetch `candidate/workbench-desktop-experience-r5`;
3. refetch `candidate/workbench-bots-rd1`;
4. read the exact files listed in Section 2;
5. discover/confirm the current host governed quarantine/evidence staging root rather than assuming a Downloads path;
6. locate the accepted RD2 top-navigation candidate source/build report/manifest on the host;
7. hash-bind that exact current RD2 source and executable;
8. verify the currently running persistent Signalproof OS Runtime identity and the governed Granite conversation path;
9. reload current `library/POWERSHELL-FAILURE-REGISTRY.yaml` if PowerShell will be generated or executed;
10. create a new isolated candidate/worktree for the next slice; never edit the accepted RD2 evidence lane in place.

Git is doctrine. This handoff is the mission/provenance index.

---

# 1. Mission

Continue the accepted Signalproof Workbench evolution from the current R5 RD2 top-navigation shell into the planned full desktop AI operations workspace.

The owner wants the strongest approved Hermes Desktop / modern agent-client functionality integrated into Workbench, but only through the Signalproof design already established.

The objective is not visual cloning.

The objective is:

> Give Signalproof Workbench the mature conversation, Bot, agent, Command Center, memory, files, preview, terminal, Git, artifacts, gateway, scheduling, group-work, HUD, and Quick Entry capabilities that make Hermes-style desktop agents useful, while keeping Signalproof branding, Signalproof-owned canonical state, and Signalproof Governor/Core authority.

---

# 2. Required Git Reading Order

Read current versions, not remembered versions.

## Public `main`

At minimum:

```text
commands/complete.md
commands/handoff-log.md
commands/build-spawn-debug.md
commands/build-git.md
commands/design-git.md
library/POWERSHELL-FAILURE-REGISTRY.yaml
library/STATE-CAPSULE.md
library/EXECUTION-PIPELINE.yaml
```

Load current applicable design/build/security/Governor/Core specialists through the repository routing rules rather than guessing names from this handoff.

## `candidate/workbench-desktop-experience-r5`

Read in full:

```text
designs/SP-WORKBENCH-DESKTOP-EXPERIENCE-R5.md
designs/SP-WORKBENCH-R5-SLICE1-RD2-TOPNAV-ACCEPTANCE.md
plans/SP-WORKBENCH-R5-HERMES-APPROVED-INTEGRATION-CODEX-PLAN.md
handoffs/SP-WORKBENCH-R5-CODEX-COMPLETE-HANDOFF.md
```

## `candidate/workbench-bots-rd1`

Read in full:

```text
designs/SP-WORKBENCH-DESKTOP-EXPERIENCE-MATRIX-RD1-R4.md
designs/SP-WORKBENCH-BOTS-SESSIONS-RD1-ARCHITECTURE.md
designs/SP-WORKBENCH-BOTS-SESSIONS-RD1-VISUAL-DIRECTION-R2.md
designs/SP-WORKBENCH-DASH-RD1-ADDENDUM.md
handoffs/SP-WORKBENCH-RD1-R4-DESIGNER-HANDOFF.md
```

Older files may contain useful continuity, but the documents above govern where conflicts exist.

---

# 3. Current Evidence-Backed Truth

## 3.1 Human accepted UX state

The owner has accepted the RD2 primary navigation arrangement and directed that it be locked in.

Accepted layout:

```text
TOP BAR:
SP Signalproof Workbench | DASH | SESSIONS | BOTS | COMMAND CENTER | MEMORY | SETTINGS | utilities

LEFT RAIL:
active-mode-specific controls/lists/filters only

CENTER:
dominant conversation/task/work canvas

RIGHT:
contextual rail/workpane / Control-Evidence now; broader Files/Preview/Terminal/Git/Artifacts/Evidence later
```

The immediately previous RD1 layout that stacked all six destinations vertically in the left rail was human-rejected.

Do not reopen the top-navigation decision unless the owner explicitly changes it or new evidence proves a material usability defect.

## 3.2 Current working functionality that must survive

Evidence from the current Workbench stream establishes a working family of behaviors including:

- Workbench launches;
- Signalproof branding;
- persistent Sessions;
- persistent Bot profiles;
- long conversation transcript and code rendering;
- normal governed conversation through Granite;
- right contextual/Control-Evidence rail behavior;
- registered-client Governor authentication;
- no direct Core/model bypass in accepted machine tests;
- authenticated read-only observation of the persistent OS Runtime;
- OS Runtime counts/state visible in Control/Evidence;
- GHL explicitly disabled behind a separate adapter/credential gate;
- Workbench close does not terminate persistent OS Runtime.

## 3.3 Protected runtime chain

Preserve:

```text
Human
  -> Signalproof Workbench
  -> Signalproof Governor
  -> Signalproof Core Runtime
  -> Signalproof Agent Runtime / Broker
  -> approved adapter / provider / worker / tool
```

The current direct governed Granite conversation remains a protected working path during migration.

Hermes is planned as a governed adapter under Agent Runtime, not as Workbench state authority.

---

# 4. Host Evidence That Must Be Reconciled First

This public handoff intentionally does not publish private machine paths.

The receiving builder must discover the configured governed quarantine/evidence root from current host/session/configuration evidence before sending execution commands.

Within that governed evidence tree, locate the accepted R5 Slice 1 RD2 top-navigation candidate lane.

Expected artifact types:

```text
src/SignalproofWorkbenchGuiRd2.cs
build/Signalproof-Workbench-R5-Slice1-RD2-TopNav.exe
BUILD-REPORT.txt
SHA256SUMS.txt
evidence/workbench-self-test.json
evidence/workbench-transport-test.json
evidence/ui-topnav-smoke.txt
evidence/human-ui-failure-rd1.txt
baseline/...
```

Do not infer the final RD2 source/executable hash from this handoff. Read them from the exact local report/manifest and independently verify them before derivation.

Useful known parent identities from the immediately preceding machine-passed RD1 candidate:

```text
R5 Slice 1 RD1 source SHA256:
01536F11A8AD90F6E7EC7AC6CD4EE6F6B5105618611688EC14C0E7972AEB6762

R5 Slice 1 RD1 EXE SHA256:
D85F22308A120EAB1A5A48AA095F5383A971FB7B5BFD08E4FDB52431B4829BF3
```

Known earlier accepted R6-OS1 source identity:

```text
F7A74ACB1F27ACD8765EC253255CFBBE18D805FF8BDE34763E8AA2727F62116E
```

Known earlier accepted R6-OS1 Workbench EXE identity:

```text
2EBC1665BC9EC48D16E5E642427477F9F6A0E0F76C5470A65E8F3B6195D4A70E
```

Known persistent OS Runtime RD3 EXE identity used in this stream:

```text
8E36CF82C4CDFDE9850BF77934A488A672E7F92DA27807F0CE3B6B6F7DF3EE98
```

Those identities are continuity anchors only. Revalidate current host state before use.

---

# 5. Protected State / Do Not Break

The next builder must preserve:

1. accepted RD2 top-navigation UX;
2. current working Sessions behavior;
3. current working Bots behavior until replaced by tested stronger implementation;
4. governed Granite conversation path;
5. Governor authentication contract;
6. Core enforcement boundary;
7. no direct Workbench -> model bypass;
8. no direct Workbench -> Core bypass;
9. no direct Workbench -> Hermes-process authority path;
10. persistent OS Runtime independence from Workbench UI lifetime;
11. Workbench OS Runtime authority remains read-only until separate actuation gate;
12. GHL remains disabled until separate adapter/credential gate;
13. production remains protected;
14. protected-main/PR controls remain binding;
15. canonical private Build Ledger append remains separate;
16. Candidate Skill activation remains separate;
17. UNKNOWN/stale/degraded/not-connected states remain truthful.

Do not greenfield rewrite the entire application merely because a cleaner architecture is possible.

Use incremental strangler migration with rollback.

---

# 6. Human UI History That Must Not Be Lost

## 6.1 R5 Slice 1 RD1 machine path

RD1 successfully proved:

- compile;
- inherited self-test;
- Governor -> Core -> Granite transport;
- six-mode route logic;
- persistent OS Runtime survival.

But the human rejected the arrangement because the six primary destinations were stacked in the left rail.

Classification:

```text
PRODUCT LOGIC: largely passed
HUMAN UX: FAIL
FAILURE TYPE: layout / information architecture
```

## 6.2 RD2 correction

RD2 moved:

```text
DASH | SESSIONS | BOTS | COMMAND CENTER | MEMORY | SETTINGS
```

to the top bar and reserved the left rail for active-mode-specific content.

The owner reviewed the resulting UI and explicitly accepted/locked it.

Do not revert to the rejected vertical six-mode rail.

---

# 7. PowerShell Guard State

PowerShell was used heavily in the candidate build/test stream.

Reload current public:

```text
library/POWERSHELL-FAILURE-REGISTRY.yaml
```

Applicable historical rule:

```text
PS-HARNESS-EVIDENCE-001
```

Known UI smoke history:

```text
Attempt 1
Method: UI Automation name lookup + InvokePattern
Result: HARNESS FAIL
Observation: DASH-like element lacked InvokePattern
Product failure proven: NO

Attempt 2
Method: UI Automation exact ControlType.Button lookup
Result: HARNESS FAIL
Observation: DASH Button not exposed through that UIA path
Product failure proven: NO

Attempt 3
Method: Win32 child HWND discovery + BM_CLICK
Result: PASS
```

Do not restart UIA attempt counts because this is a fresh chat or a renamed script.

For WinForms navigation smoke, the proven Win32 child-window method is the current successful deterministic method unless conditions materially change.

Every new/modified `.ps1` must still follow current registry rules, exact-final parser preflight, final identity binding, and failure classification.

---

# 8. Hermes Capability Disposition

The owner wants the approved Hermes feature family brought into Workbench.

Do not interpret that as permission to port the entire Hermes GUI or state model.

Approved high-level families from the R4 matrix include:

- mature Sessions/chat experience;
- streaming/tool/run activity;
- Bots roster and richer Bot configuration;
- Bot routines;
- group chats;
- visible Bot-to-Bot messaging/delegation;
- Command Center;
- Memory Graph/List/Timeline;
- Context/Files/Preview right workpane;
- Terminal;
- Git review/worktrees;
- Artifacts;
- context usage / HUD;
- Quick Entry;
- providers/models/gateways;
- Tools/MCP/Skills management;
- multi-gateway/local/remote execution profiles;
- command palette / keyboard workflow;
- later voice/multiwindow polish.

Rejected/forbidden interpretation:

- Hermes branding as Signalproof branding;
- Hermes canonical state replacing Signalproof state;
- unrestricted/YOLO execution as a bypass of Governor/Core;
- silent credential/context/tool inheritance;
- fabricated live worker state;
- required Hermes Cloud dependency;
- arbitrary file/network access simply because Hermes can do it.

---

# 9. Canonical Next Implementation Sequence

Use the detailed plan in:

`plans/SP-WORKBENCH-R5-HERMES-APPROVED-INTEGRATION-CODEX-PLAN.md`

High-level order:

```text
Gate 0  reconcile exact accepted RD2 candidate
Gate 1  structural internal seams without UX regression
Gate 2  Sessions maturity
Gate 3  canonical Bot model + richer Bot editor
Gate 4  Signalproof Agent Runtime / Broker
Gate 5  minimum governed Hermes adapter
Gate 6  Command Center live binding
Gate 7  right workpane Context / Files / Preview / Artifacts
Gate 8  governed Terminal / execution environments
Gate 9  Git review / worktrees
Gate 10 Memory service + Graph/List/Timeline
Gate 11 routines / Scheduler
Gate 12 group chat / inter-Bot delegation
Gate 13 providers / gateways / MCP / tools settings
Gate 14 Dash live operational binding
Gate 15 HUD / Quick Entry / command palette / keyboard polish
Gate 16 voice / multiwindow / advanced polish
```

Do not skip Agent Runtime and wire advanced Bot/Command Center UI directly to Hermes-specific state.

---

# 10. First `/dsp complete` Envelope for Receiving Builder

After Gate 0 reconciliation, establish an exact envelope equivalent to:

```text
COMPLETE AUTHORIZATION ENVELOPE
Workstream: Signalproof Workbench R5 Hermes-Approved Functionality Integration
Objective: implement the next bounded integration slice from the accepted RD2 shell toward the Codex plan while preserving governed working behavior
Git basis: current public main + current candidate/workbench-desktop-experience-r5 + current candidate/workbench-bots-rd1 + exact host RD2 candidate identity
Protected state: accepted RD2 top-nav shell, governed Granite path, Governor/Core boundaries, persistent OS Runtime, production baseline
Acceptance target: slice-specific automated PASS + exact artifact identity + owner UI gate for material UX change
Allowed authority: owner-authorizable build/investigate/debug/test/verify/recovery/learning work within this exact candidate stream
Known failures / retry memory: preserve PowerShell registry + UI smoke attempt history above
Recovery: preserve accepted RD2 lane; derive all new work in isolated candidate/worktree
Excluded authority: production deployment/public release, protected-main bypass, canonical private Build Ledger append, Candidate Skill activation, credentials not already authorized, privilege/security-control changes, legal/license bypass
Consumption: owner accepts bounded slice, material workstream changes, envelope invalidates, or true STOP
Status: AUTHORIZED after Gate 0 exact-current-state reconciliation; otherwise AUTHORIZED-PENDING-PRECONDITION
```

Do not ask the owner to re-authorize ordinary build/debug/test steps inside that established envelope.

---

# 11. Builder Architecture Strategy

Prefer:

```text
accepted behavior
 -> extract interface/service seam
 -> add Signalproof-owned canonical contract
 -> bind new normalized service
 -> add adapter
 -> add/upgrade UI consuming normalized state
 -> prove parity/stronger behavior
 -> retire old inline path only when evidence supports it
```

Avoid:

```text
rewrite UI + rewrite runtime + add Hermes + change persistence + change security all at once
```

Keep candidate slices small enough that a failure has a meaningful fault domain and a credible rollback.

---

# 12. Testing / Evidence Expectations

For each applicable slice capture separately:

- Git/ref/source identity;
- protected baseline identity;
- source/static contract checks;
- compile/build result;
- persistence/state migration result;
- Governor authentication/policy result;
- Core enforcement result;
- model/adapter transport result;
- Agent Runtime Task/Run lifecycle result;
- cancellation/error classification where applicable;
- reopen/process-independence result;
- UI navigation/resize/accessibility result;
- human visual/usability result;
- evidence/provenance manifest;
- protected-baseline postflight.

Do not collapse concept PASS, machine PASS, and human UI PASS into one claim.

---

# 13. Licensing / Security Review Before Hermes Source Adoption

Before reusing actual Hermes source rather than reimplementing an interaction:

1. inspect current upstream repository and license;
2. inspect relevant file-level provenance and dependencies;
3. determine whether adaptation is compatible with Signalproof licensing and distribution plans;
4. identify security/credential/execution implications;
5. document ADOPT / ADAPT / INTEGRATE / DEFER / REJECT disposition;
6. prefer independent implementation when copying upstream code adds avoidable license or architectural coupling.

No convenience feature may weaken Governor/Core, quarantine/evidence, rollback, or provenance controls.

---

# 14. Build Ledger Boundary

This handoff and candidate Git commits are continuity evidence.

They do not claim:

- canonical private Build Ledger sequence;
- canonical milestone closeout;
- deployment;
- public release;
- production activation.

If private Build Ledger access is available in the receiving environment, retrieve only task-relevant current continuity and reconcile it. If unavailable, state `UNAVAILABLE`; do not guess chronology.

---

# 15. Result Branches

## If Gate 0 exact state matches

Proceed to Gate 1 under `/dsp complete`.

## If local RD2 report/manifest is missing

STOP before mutation and perform bounded read-only discovery/recovery. Do not derive from an older RD1 source by assumption.

## If Git has drifted

Reconcile the newer current command/design contracts first. Do not blindly enforce this handoff over newer Git authority.

## If current Granite path fails before new work

Classify/recover baseline first. Do not add Hermes functionality on top of an already-regressed protected path.

## If a proposed Hermes feature requires authority bypass

REJECT or redesign the integration. Do not implement the bypass.

## If a material owner UX choice arises that the R5/R4 design does not resolve

Return the smallest decision to the owner. Ordinary build/debug choices remain inside `/dsp complete`.

---

# 16. STOP Conditions

STOP when:

- exact accepted RD2 source/executable identity cannot be established;
- protected state would be overwritten;
- current Git materially contradicts transferred authority;
- a same-failure retry would repeat without materially changed conditions;
- licensing/provenance is materially unresolved for code reuse;
- security/privacy boundary is unclear;
- required credentials or privileges are not authorized;
- the design would bypass Governor/Core;
- a UI would fabricate connected/running/healthy/authorized state;
- production/release/protected-main/canonical-ledger authority would be required;
- recovery is no longer credible.

A normal failed compile/test/adapter/UI attempt is not by itself a terminal STOP when a bounded evidence-supported correction remains available.

---

# 17. Handoff Completion State

```text
TRANSFER: READY
R5 RD2 TOP NAVIGATION: OWNER ACCEPTED / LOCKED
CURRENT SHELL: PRESERVE
CURRENT GRANITE PATH: PRESERVE / REVERIFY AT RECEIVE
PERSISTENT OS RUNTIME: PRESERVE / REVERIFY AT RECEIVE
HERMES FEATURE FAMILY: APPROVED THROUGH R4 DISPOSITION MATRIX
HERMES AS AUTHORITY: REJECTED
NEXT WORKSTREAM: R5 HERMES-APPROVED FUNCTIONALITY INTEGRATION
NEXT ROUTE: /dsp complete
FIRST ACTION: GATE 0 EXACT HOST/GIT RECONCILIATION
PRODUCTION MUTATION: NOT AUTHORIZED
CANONICAL PRIVATE BUILD LEDGER APPEND: NOT CLAIMED
```
